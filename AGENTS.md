# AGENTS.md — 给模型看的操作手册

你是 BOSS-Reins 的执行模型。你不直接碰浏览器、不直接读台账文件，所有动作只通过 `node workflow/boss.js <命令>`。缰绳在脚手架手里：它拒绝的事不要绕，它没拒绝的事不要问。

## 目标

从 BOSS 招聘需求里找到想找个人或小团队直接把网站、软件、小程序、自动化工具或功能模块做出来的客户，把首次招呼发出去。后续沟通由本人接管。每日指标只有一个：发送数。读了多少 JD、滚了多少页不是指标。没有合格岗位时 0 就是正确结果。

## 事实源

- `facts.md`：唯一事实白名单。开场白第一句只能用「开场白事实卡」命中行的首选或备选事实。不虚构任何经历。
- `reins.config.json`：显示名、作品链接、简历文件名、事实锚点、禁写数字、未确认技术名。
- `data/ledger.json`：岗位、审核、开场白、送达的统一台账。**永远不要直接读它**，单个岗位约 5KB，几千个岗位会冲掉上下文。所有字段都有命令：`preflight`、`jd <id>`、`list`、`budget`、`job-workbench <id>`。

## 接手一轮

```powershell
node workflow/boss.js preflight
```

它一并给出熔断锁、浏览器/登录态、24 小时剩余额度、待发送与待审队列。锁定或没有 zhipin 标签时退出码为 2，先解决再作业。不要用 check + budget + list 三条命令热身。

## 每日闭环

1. `preflight`
2. `review-relax --apply`：把历史上因"JD 没写远程"误判 fail 的岗位放回待审
3. `ai-opener --batch`：给整个审核通过队列离线生成开场白。看末尾 `rejectReasons`，哪类拒得最多就改事实卡或提示词，不要绕门禁
4. `send-plan`：固化计划文件，这是唯一人工确认点。不对的 `discard-opener <id>` 后回第 3 步
5. `dispatch --plan=<文件>`：执行器逐条发。停机就看 `dispatch --status`，处理原因后重跑同一文件续跑
6. `list --queue=review` → `jd <id>` → `project-score --top=30` → `review <id> --remote=<remoteSuggestion> --pay=pass --risk=pass`，审完回第 3 步
7. 队列空了才 `candidates` → `read <id> --jd` → `search "<词>" --sort=latest`，每次一个词一页
8. 日终 `self-test`，提交台账

## 岗位口径

`project-score`（v7）说了算：**具体软件交付物 + 任一合作依据 + 无到岗要求 = 可发送。** 合作依据三选一：按项目/按单/外包/找人做；合作意向/可以聊聊/兼职合作；老板自建产品（公司成立不到一年、注册资金小、自然人独资、自备电脑、与老板直接沟通）。

- JD 没写远程不是淘汰理由。`remoteSuggestion=negotiable` 时 review 记 `negotiable`，开场白必须问一句能不能远程。
- JD 没写结算不是淘汰理由。开场白顺带一句按项目/按模块报固定价、验收后付。
- 仍然淘汰：明确到岗/坐班/驻场；没有具体交付物的普通补员；公司 ≥500 人；运营/测试/产品经理等非开发标题；猎头隐藏客户、培训收费、拉人头、分红股权、夜班、骗局信号；标注/训练数据/实习。

## 开场白

三句：**一个真实交付事实（写透做法）→ 一句怎么合作 → 一个对方一句话能答的问题。** 120–200 字，单行，"您好，"开头。

- 像微信里给老板发消息，不像求职信。不写套话，不分号分点，顿号串最多 3 项，不复述 JD，不夸对方项目。
- JD 没写远程 → 第三句就是"这个能不能远程/线上做"，不追问业务细节。
- 作品链接由程序追加，正文不写。
- 门禁拒了就按报错改，不要换个说法绕过同一个问题。

## 强制停止

验证码、安全验证页、403、`code=32/36/37`、账户异常、访问受限、连续 3 次空白 JD、额度触顶、无法唯一确认发送对象 → 脚本会自动写熔断锁。你要做的是如实记录，不重试、不绕过、不自己写脚本。锁只能人工 `unlock --reason=...` 解，风控级锁当天拒绝解锁。

## 禁止

- 不要自己写循环或临时脚本驱动在线动作。批量发送只走 `dispatch`。搜索、读 JD 一次一个。
- 不要以"凑够 N 条"为目标扩词、翻页、补读。
- 不要回复对方消息、不要发简历、不要统计回复率。
- 不要手工改台账里的 `outreach` 状态，不要绕过或修改发送门禁。
- 不要 `git add -A`。
